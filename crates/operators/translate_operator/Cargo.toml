[package]
name = "translate_operator"
version = "0.2.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
volumetric_abi = { path = "../../volumetric_abi" }
model_wrap_core = { path = "../../model_wrap_core" }
serde = { version = "1", features = ["derive"] }
ciborium = "0.2"
