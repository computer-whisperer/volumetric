[package]
name = "threemf_import_operator"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
volumetric_abi = { path = "../../volumetric_abi" }
threemf_core = { path = "../../threemf_core" }
serde = { version = "1", features = ["derive"] }
ciborium = "0.2"
