[package]
name = "survey_operator"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
volumetric_abi = { path = "../../volumetric_abi" }
cv_core = { path = "../../cv_core" }
serde = { version = "1", features = ["derive"] }
ciborium = "0.2"
