[package]
name = "fea_density_operator"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
volumetric_abi = { path = "../../volumetric_abi" }
fea_core = { path = "../../fea_core" }
serde = { version = "1", features = ["derive"] }
ciborium = "0.2"
walrus = "0.26"
