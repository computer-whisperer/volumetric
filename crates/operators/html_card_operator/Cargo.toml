[package]
name = "html_card_operator"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
volumetric_abi = { path = "../../volumetric_abi" }
outline_model_core = { path = "../../outline_model_core", features = ["emit"] }
html_card_core = { path = "../../html_card_core" }
serde = { version = "1", features = ["derive"] }
ciborium = "0.2"
