[package]
name = "volume_import_operator"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
volumetric_abi = { path = "../../volumetric_abi" }
nrrd_core = { path = "../../nrrd_core" }
ndfield_model_core = { path = "../../ndfield_model_core", features = ["emit"] }
serde = { version = "1", features = ["derive"] }
ciborium = "0.2"
