[package]
name = "mesh_remaster_operator"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
volumetric_abi = { path = "../../volumetric_abi" }
mesh_edit_core = { path = "../../mesh_edit_core" }
serde = { version = "1", features = ["derive"] }
ciborium = "0.2"
