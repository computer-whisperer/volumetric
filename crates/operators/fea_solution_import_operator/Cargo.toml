[package]
name = "fea_solution_import_operator"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
volumetric_abi = { path = "../../volumetric_abi" }
fea_core = { path = "../../fea_core" }
fea_bundle = { path = "../../fea_bundle" }
serde = { version = "1", features = ["derive"] }
ciborium = "0.2"
