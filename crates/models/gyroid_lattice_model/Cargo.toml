[package]
name = "gyroid_lattice_model"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]
test = false
doctest = false

[dependencies]
libm = "0.2"

[build-dependencies]
volumetric_abi = { path = "../../volumetric_abi" }
