[package]
name = "ndfield_model_core"
version = "0.1.0"
edition = "2024"

[dependencies]
walrus = { version = "0.26", optional = true }
model_wrap_core = { path = "../model_wrap_core", optional = true }

[features]
emit = ["dep:walrus", "dep:model_wrap_core"]
