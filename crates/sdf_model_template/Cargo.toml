[package]
name = "sdf_model_template"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
ndfield_model_core = { path = "../ndfield_model_core" }
volumetric_abi = { path = "../volumetric_abi" }
